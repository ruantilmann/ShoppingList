# Skill: next-devtools

Use Next.js DevTools MCP for runtime diagnostics and route discovery.

## Workflow
1. Call `next-devtools_init` at the start of a Next.js session.
2. Use `next-devtools_nextjs_index` to discover servers and tools.
3. Use `next-devtools_nextjs_call` for runtime checks (errors, routes, logs).
