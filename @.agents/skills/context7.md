# Skill: context7

Use Context7 to pull authoritative docs and best practices for a library.

## Workflow
1. Call `context7_resolve-library-id` with the library name and task.
2. Use the returned ID with `context7_query-docs` for specific guidance.
3. Summarize actionable best practices and relevant APIs.
