# Skill: better-auth

Use Better Auth MCP to validate security and auth best practices.

## Workflow
1. Use `better-auth_search-better-auth-docs` for specific topics.
2. Summarize security considerations (trusted origins, CSRF, cookies).
3. Highlight mismatches between config and UI flows.
